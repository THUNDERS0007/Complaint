<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HISTORY</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        .card{
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center mt-3 mb-3"><%= title %></h1>

        <div class="card mb-5">
            <div class="card-header">View complaint history</div>
            <div class="card-body">
                <form id="complaintForm" method="post" action="/history/show">
                
                    <div class="mb-3">
                        <label for="complaint" class="form-label">Complaint</label>
                            <select name="complaint" id="complaint" class="form-control">
                                <option value="0">Select Complaint</option>
                                <% for(var count = 0; count < sampleData.length; count++) { %>
                                    <option value="<%= sampleData[count].complaint_id %>"><%= sampleData[count].complaint_id %> - <%= sampleData[count].dept_name %> (<%= sampleData[count].complaint %>), <%= sampleData[count].issue_detail %></option>
                                <% } %>
                            </select>
                        </select>
                    </div>
                    <input type="submit" class="btn btn-outline-success btn-sm" value="Show">
                    <input type="reset" class="btn btn-outline-success btn-sm" value="Clear">
                </form>
            </div>
        </div>
            <div class="card">
                <div class="card-header">Search result:</div>
                <div class="card-body">
                    
                </div>
            </div>
        
</body>

</html>